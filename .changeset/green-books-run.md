---
---

chore(sdk): tooling-only change — a post-fetch spec-patch layer that repairs
known upstream Emporix spec defects before type generation. No published
package output changes, so no version bump. The generated-type updates ship
via the daily api-sync PR.
