---
---

CI-only: authenticate the release workflow via a GitHub App token so the
auto-generated release PR triggers required checks, and skip the changeset gate
on that release PR. No package changes.
