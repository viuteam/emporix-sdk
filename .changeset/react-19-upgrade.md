---
---

Upgrade the development + example toolchain to React 19 (and Next 15 in the
Next.js example). No published-package changes: `@viu/emporix-sdk` and
`@viu/emporix-sdk-react` source and public API are unchanged — the React peer
range already allowed `^18 || ^19` — so no version bump is required. This empty
changeset records the change and satisfies the changeset check without a release.
