/**
 * Moved to `@viu/emporix-sdk` (`core/browser-storage.ts`). Internal helper
 * shared by the localStorage and sessionStorage backends; re-exported only so an
 * existing internal import keeps resolving.
 */
export { fromWebStorage } from "@viu/emporix-sdk";
