/**
 * Moved to `@viu/emporix-sdk` (`core/browser-storage.ts`) — `sessionStorage` is
 * not a React API, and `@viu/emporix-sdk-angular` needs the same backend.
 * Re-exported: this name has always been part of this package's public surface.
 */
export { createSessionStorage } from "@viu/emporix-sdk";
