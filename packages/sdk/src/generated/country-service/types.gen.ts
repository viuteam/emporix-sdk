// AUTO-GENERATED — do not edit
// This file is auto-generated by @hey-api/openapi-ts

/**
 * Schema for API specified errors.
 */
export type ErrorMessage = {
    /**
     * Original HTTP error reason.
     */
    status: string;
    /**
     * Original HTTP error code, should be consistent with the response HTTP code.
     */
    code: number;
    /**
     * Descriptive error message for debugging.
     */
    message?: string;
    /**
     * List of problems causing this error.
     */
    details?: Array<string>;
};

export type Metadata = {
    /**
     * Date and time when the document was created.
     */
    createdAt: string;
    /**
     * Date and time when the document was last modified.
     */
    modifiedAt: string;
    /**
     * Version of the document.
     */
    version: number;
};

export type Country = {
    /**
     * The country code which consists of two capital letters and is compliant with ISO-3166-1 specification.
     */
    code?: string;
    /**
     * Localized name of the country. The value is returned as a map where the key is a language code and the value is a translation for the given language.
     */
    name?: {
        [key: string]: string;
    };
    /**
     * A list of regions to which the given country belongs to.
     */
    regions?: Array<string>;
    /**
     * The field indicating whether the given country is active for the tenant or not. Only active countries are visible in the system.
     */
    active?: boolean;
    metadata?: Metadata;
};

export type Region = {
    /**
     * The region code.
     */
    code?: string;
    /**
     * The localized name of the region. The value is returned as a map where the key is a language code and the value is a translation for the given language.
     */
    name?: {
        [key: string]: string;
    };
    metadata?: Metadata;
};

export type CountryUpdate = {
    /**
     * The field indicates whether the given country is active for the tenant or not. Only active countries are visible in the system.
     */
    active?: boolean;
    metadata: {
        /**
         * Version of the document. The version is used for optimistic locking mechanism during the update request.
         */
        version: number;
    };
};

/**
 * The tenant that the caller is acting upon.
 *
 * Please note that this value is always lowercase.
 *
 */
export type TraitTenant = string;

/**
 * The page number to be retrieved where the size of the pages must be specified by the pageSize parameter.
 * The number of the first page is 1.
 *
 */
export type TraitPagedPageNumber = number;

/**
 * Fields to sort the response data by, following the order of the parameters from left to right.
 *
 * * Can contain multiple fields in the format: `fieldName:sortDirection`, separated by commas.
 *
 * * The colon with the sort direction parameter is optional.
 *
 * * Descending order is used only if the sort direction equals `desc` or `DESC`.
 *
 * * Ascending order is assumed in all other cases.
 *
 * While sorting by a localized field, the following rules apply:
 *
 * * If a localized field includes a language code, that language is used.
 * Example: `name.en:ASC` → English (`en`) is used for sorting the `name` property.
 * The language suffix takes precedence over the `Accept-Language` header.
 *
 * * If a localized field does not include a language code, the `Accept-Language` header is used.
 * Example: `name:ASC` with `Accept-Language: de` → German (`de`) is used for sorting.
 *
 * * If a localized field has no language code and the `Accept-Language` header contains `*`,
 * the default language from the configuration service is used.
 * Example: `name:ASC`, `Accept-Language: *`, default language = `pl` → Polish (`pl`) is used.
 *
 * * If a localized field has no language code and there is no `Accept-Language` header,
 * the default language from the configuration service is used.
 *
 */
export type TraitSort = string;

/**
 * The number of documents being retrieved on the page.
 *
 */
export type TraitPagedPageSize = number;

/**
 * List of language codes acceptable for the response. You can specify factors that indicate which language should be retrieved if the one with a higher factor was not found in the localized fields.
 * * If the header is set to a particular language or a list of languages, all localized fields are retrieved as maps with the first matched language only.
 * * If the header is set to `*`, all localized fields are retrieved as maps of translations, where the keys are language codes and values are the fields in their respective languages.
 * * If the header is empty, localized fields are retrieved in the default language defined in the Configuration Service.
 *
 */
export type TraitAcceptLanguageHeader = string;

/**
 * In order to get information how many entities meet a filter requirements, X-Total-Count header has been introduced. The header is optional and its default value is false. It the header is provided and it is true then total count is returned in the X-Total-Count response header. In both cases (X-Total-Count true, false or not provided), the response body has the same format (array of entities). In other words, the information about total count is returned on demand, depending of an existence of X-Total-Count header in a request. Therefore, the X-Total-Count header is not returned if an API consumer didn’t ask for it.
 */
export type TraitXTotalCountHeader = boolean;

/**
 * Two-letter country code.
 */
export type TraitCountryCodePath = string;

/**
 * The region code.
 */
export type TraitRegionCodePath = string;

/**
 * Country codes. When specified, only countries containing the specified code are returned.
 *
 * **Note**: When filtering the results with this field, the `contains` operator is used. The operator is case-insensitive.
 *
 */
export type TraitCountryCodeQueryParam = string;

/**
 * Country names. When specified, only countries containing the specified names are returned.
 *
 * **Important**: See how the `Accept-Language` header influences this parameter:
 * * If The `Accept-Language` header is set to `*`, you need to specify a language in this parameter.
 *
 * For example, if the `Accept-Language` header is set to `*` and you want to find English names containing the word "Isle", this parameter should be set to "name.en=Isle"
 *
 * * If the `Accept-Language` header is set to a particular language or not specified, you do not need to specify a language in this parameter.
 *
 * For example, if the `Accept-Language` header is set to `en` and you want to find names containing the word "Isle", this parameter should be set to "name=Isle"
 *
 * **Note**: When filtering the results with this field, the `contains` operator is used. The operator is case-insensitive.
 *
 */
export type TraitCountryNameQueryParam = string;

/**
 * Region names. When specified, only regions containing the specified names are returned.
 *
 * **Important**: See how the `Accept-Language` header influences this parameter:
 * * If The `Accept-Language` header is set to `*`, you need to specify a language in this parameter.
 *
 * For example, if the `Accept-Language` header is set to `*` and you want to find English names containing the word "America", this parameter should be set to "name.en=America"
 *
 * * If the `Accept-Language` header is set to a particular language or not specified, you do not need to specify a language in this parameter.
 *
 * For example, if the `Accept-Language` header is set to `en` and you want to find names containing the word "America", this parameter should be set to "name=America"
 *
 * **Note**: When filtering the results with this field, the `contains` operator is used. The operator is case-insensitive.
 *
 */
export type TraitRegionNameQueryParam = string;

/**
 * Country active status, expressed as `true` or `false`. When specified, only countries with the active status are returned.
 *
 * **Note**: When filtering the results with this field, the `equals` operator is used.
 *
 */
export type TraitCountryActiveQueryParam = string;

/**
 * Country region codes. When specified, only countries containing the specified region codes are returned.
 *
 * **Note**: When filtering the results with this field, the `contains` operator is used.
 *
 */
export type TraitCountryRegionsQueryParam = string;

/**
 * Region codes. When specified, only regions containing the specified codes are returned.
 *
 * **Note**: When filtering the results with this field, the `contains` operator is used.
 *
 */
export type TraitRegionCodeQueryParam = string;

/**
 * List of language codes acceptable for the response. You can specify factors that indicate which language should be retrieved if the one with a higher factor was not found in the localized fields.
 *
 * **Note**: If not specified, localized fields are retrieved in English.
 *
 */
export type PriceApiTraitAcceptLanguageHeader = string;

/**
 * The X-Version header with value v2 is required for the endpoint.
 */
export type TraitXVersionHeader = string;

export type GetCountryListAllCountriesData = {
    body?: never;
    headers: {
        /**
         * List of language codes acceptable for the response. You can specify factors that indicate which language should be retrieved if the one with a higher factor was not found in the localized fields.
         * * If the header is set to a particular language or a list of languages, all localized fields are retrieved as maps with the first matched language only.
         * * If the header is set to `*`, all localized fields are retrieved as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the header is empty, localized fields are retrieved in the default language defined in the Configuration Service.
         *
         */
        'Accept-Language'?: string;
        /**
         * In order to get information how many entities meet a filter requirements, X-Total-Count header has been introduced. The header is optional and its default value is false. It the header is provided and it is true then total count is returned in the X-Total-Count response header. In both cases (X-Total-Count true, false or not provided), the response body has the same format (array of entities). In other words, the information about total count is returned on demand, depending of an existence of X-Total-Count header in a request. Therefore, the X-Total-Count header is not returned if an API consumer didn’t ask for it.
         */
        'X-Total-Count'?: boolean;
        /**
         * The X-Version header with value v2 is required for the endpoint.
         */
        'X-Version': string;
    };
    path: {
        /**
         * The tenant that the caller is acting upon.
         *
         * Please note that this value is always lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * The page number to be retrieved where the size of the pages must be specified by the pageSize parameter.
         * The number of the first page is 1.
         *
         */
        pageNumber?: number;
        /**
         * The number of documents being retrieved on the page.
         *
         */
        pageSize?: number;
        /**
         * Fields to sort the response data by, following the order of the parameters from left to right.
         *
         * * Can contain multiple fields in the format: `fieldName:sortDirection`, separated by commas.
         *
         * * The colon with the sort direction parameter is optional.
         *
         * * Descending order is used only if the sort direction equals `desc` or `DESC`.
         *
         * * Ascending order is assumed in all other cases.
         *
         * While sorting by a localized field, the following rules apply:
         *
         * * If a localized field includes a language code, that language is used.
         * Example: `name.en:ASC` → English (`en`) is used for sorting the `name` property.
         * The language suffix takes precedence over the `Accept-Language` header.
         *
         * * If a localized field does not include a language code, the `Accept-Language` header is used.
         * Example: `name:ASC` with `Accept-Language: de` → German (`de`) is used for sorting.
         *
         * * If a localized field has no language code and the `Accept-Language` header contains `*`,
         * the default language from the configuration service is used.
         * Example: `name:ASC`, `Accept-Language: *`, default language = `pl` → Polish (`pl`) is used.
         *
         * * If a localized field has no language code and there is no `Accept-Language` header,
         * the default language from the configuration service is used.
         *
         */
        sort?: string;
        /**
         * Country names. When specified, only countries containing the specified names are returned.
         *
         * **Important**: See how the `Accept-Language` header influences this parameter:
         * * If The `Accept-Language` header is set to `*`, you need to specify a language in this parameter.
         *
         * For example, if the `Accept-Language` header is set to `*` and you want to find English names containing the word "Isle", this parameter should be set to "name.en=Isle"
         *
         * * If the `Accept-Language` header is set to a particular language or not specified, you do not need to specify a language in this parameter.
         *
         * For example, if the `Accept-Language` header is set to `en` and you want to find names containing the word "Isle", this parameter should be set to "name=Isle"
         *
         * **Note**: When filtering the results with this field, the `contains` operator is used. The operator is case-insensitive.
         *
         */
        name?: string;
        /**
         * Country codes. When specified, only countries containing the specified code are returned.
         *
         * **Note**: When filtering the results with this field, the `contains` operator is used. The operator is case-insensitive.
         *
         */
        code?: string;
        /**
         * Country active status, expressed as `true` or `false`. When specified, only countries with the active status are returned.
         *
         * **Note**: When filtering the results with this field, the `equals` operator is used.
         *
         */
        active?: string;
        /**
         * Country region codes. When specified, only countries containing the specified region codes are returned.
         *
         * **Note**: When filtering the results with this field, the `contains` operator is used.
         *
         */
        regions?: string;
    };
    url: '/country/{tenant}/countries';
};

export type GetCountryListAllCountriesErrors = {
    /**
     * Request was syntactically incorrect. Details are provided in the response payload.
     */
    400: ErrorMessage;
    /**
     * Internal Service Error occurred.
     */
    500: ErrorMessage;
};

export type GetCountryListAllCountriesError = GetCountryListAllCountriesErrors[keyof GetCountryListAllCountriesErrors];

export type GetCountryListAllCountriesResponses = {
    /**
     * The request was successful. A list of countries is returned.
     */
    200: Array<Country>;
};

export type GetCountryListAllCountriesResponse = GetCountryListAllCountriesResponses[keyof GetCountryListAllCountriesResponses];

export type GetCountryRetrieveCountryData = {
    body?: never;
    headers: {
        /**
         * List of language codes acceptable for the response. You can specify factors that indicate which language should be retrieved if the one with a higher factor was not found in the localized fields.
         *
         * **Note**: If not specified, localized fields are retrieved in English.
         *
         */
        'Accept-Language'?: string;
        /**
         * The X-Version header with value v2 is required for the endpoint.
         */
        'X-Version': string;
    };
    path: {
        /**
         * The tenant that the caller is acting upon.
         *
         * Please note that this value is always lowercase.
         *
         */
        tenant: string;
        /**
         * Two-letter country code.
         */
        countryCode: string;
    };
    query?: never;
    url: '/country/{tenant}/countries/{countryCode}';
};

export type GetCountryRetrieveCountryErrors = {
    /**
     * Request was syntactically incorrect. Details are provided in the response payload.
     */
    400: ErrorMessage;
    /**
     * The requested resource does not exist.
     */
    404: ErrorMessage;
    /**
     * Internal Service Error occurred.
     */
    500: ErrorMessage;
};

export type GetCountryRetrieveCountryError = GetCountryRetrieveCountryErrors[keyof GetCountryRetrieveCountryErrors];

export type GetCountryRetrieveCountryResponses = {
    /**
     * The request was successful. The requested country is returned.
     */
    200: Country;
};

export type GetCountryRetrieveCountryResponse = GetCountryRetrieveCountryResponses[keyof GetCountryRetrieveCountryResponses];

export type PatchCountryUpdateCountryData = {
    body?: CountryUpdate;
    headers: {
        /**
         * The X-Version header with value v2 is required for the endpoint.
         */
        'X-Version': string;
    };
    path: {
        /**
         * The tenant that the caller is acting upon.
         *
         * Please note that this value is always lowercase.
         *
         */
        tenant: string;
        /**
         * Two-letter country code.
         */
        countryCode: string;
    };
    query?: never;
    url: '/country/{tenant}/countries/{countryCode}';
};

export type PatchCountryUpdateCountryErrors = {
    /**
     * Request was syntactically incorrect. Details are provided in the response payload.
     */
    400: ErrorMessage;
    /**
     * Given request is unauthorized - the authorization token is invalid or has expired. Details are provided in the response payload.
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Given authorization scopes are not sufficient and do not match scopes required by the endpoint.
     */
    403: ErrorMessage;
    /**
     * The requested resource does not exist.
     */
    404: ErrorMessage;
    /**
     * Internal Service Error occurred.
     */
    500: ErrorMessage;
};

export type PatchCountryUpdateCountryError = PatchCountryUpdateCountryErrors[keyof PatchCountryUpdateCountryErrors];

export type PatchCountryUpdateCountryResponses = {
    /**
     * No Content
     */
    204: void;
};

export type PatchCountryUpdateCountryResponse = PatchCountryUpdateCountryResponses[keyof PatchCountryUpdateCountryResponses];

export type GetCountryListAllRegionsData = {
    body?: never;
    headers: {
        /**
         * List of language codes acceptable for the response. You can specify factors that indicate which language should be retrieved if the one with a higher factor was not found in the localized fields.
         * * If the header is set to a particular language or a list of languages, all localized fields are retrieved as maps with the first matched language only.
         * * If the header is set to `*`, all localized fields are retrieved as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the header is empty, localized fields are retrieved in the default language defined in the Configuration Service.
         *
         */
        'Accept-Language'?: string;
        /**
         * In order to get information how many entities meet a filter requirements, X-Total-Count header has been introduced. The header is optional and its default value is false. It the header is provided and it is true then total count is returned in the X-Total-Count response header. In both cases (X-Total-Count true, false or not provided), the response body has the same format (array of entities). In other words, the information about total count is returned on demand, depending of an existence of X-Total-Count header in a request. Therefore, the X-Total-Count header is not returned if an API consumer didn’t ask for it.
         */
        'X-Total-Count'?: boolean;
        /**
         * The X-Version header with value v2 is required for the endpoint.
         */
        'X-Version': string;
    };
    path: {
        /**
         * The tenant that the caller is acting upon.
         *
         * Please note that this value is always lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * The page number to be retrieved where the size of the pages must be specified by the pageSize parameter.
         * The number of the first page is 1.
         *
         */
        pageNumber?: number;
        /**
         * The number of documents being retrieved on the page.
         *
         */
        pageSize?: number;
        /**
         * Fields to sort the response data by, following the order of the parameters from left to right.
         *
         * * Can contain multiple fields in the format: `fieldName:sortDirection`, separated by commas.
         *
         * * The colon with the sort direction parameter is optional.
         *
         * * Descending order is used only if the sort direction equals `desc` or `DESC`.
         *
         * * Ascending order is assumed in all other cases.
         *
         * While sorting by a localized field, the following rules apply:
         *
         * * If a localized field includes a language code, that language is used.
         * Example: `name.en:ASC` → English (`en`) is used for sorting the `name` property.
         * The language suffix takes precedence over the `Accept-Language` header.
         *
         * * If a localized field does not include a language code, the `Accept-Language` header is used.
         * Example: `name:ASC` with `Accept-Language: de` → German (`de`) is used for sorting.
         *
         * * If a localized field has no language code and the `Accept-Language` header contains `*`,
         * the default language from the configuration service is used.
         * Example: `name:ASC`, `Accept-Language: *`, default language = `pl` → Polish (`pl`) is used.
         *
         * * If a localized field has no language code and there is no `Accept-Language` header,
         * the default language from the configuration service is used.
         *
         */
        sort?: string;
        /**
         * Region names. When specified, only regions containing the specified names are returned.
         *
         * **Important**: See how the `Accept-Language` header influences this parameter:
         * * If The `Accept-Language` header is set to `*`, you need to specify a language in this parameter.
         *
         * For example, if the `Accept-Language` header is set to `*` and you want to find English names containing the word "America", this parameter should be set to "name.en=America"
         *
         * * If the `Accept-Language` header is set to a particular language or not specified, you do not need to specify a language in this parameter.
         *
         * For example, if the `Accept-Language` header is set to `en` and you want to find names containing the word "America", this parameter should be set to "name=America"
         *
         * **Note**: When filtering the results with this field, the `contains` operator is used. The operator is case-insensitive.
         *
         */
        name?: string;
        /**
         * Region codes. When specified, only regions containing the specified codes are returned.
         *
         * **Note**: When filtering the results with this field, the `contains` operator is used.
         *
         */
        code?: string;
    };
    url: '/country/{tenant}/regions';
};

export type GetCountryListAllRegionsErrors = {
    /**
     * Request was syntactically incorrect. Details are provided in the response payload.
     */
    400: ErrorMessage;
    /**
     * Internal Service Error occurred.
     */
    500: ErrorMessage;
};

export type GetCountryListAllRegionsError = GetCountryListAllRegionsErrors[keyof GetCountryListAllRegionsErrors];

export type GetCountryListAllRegionsResponses = {
    /**
     * The request was successful. A list of regions is returned.
     */
    200: Array<Region>;
};

export type GetCountryListAllRegionsResponse = GetCountryListAllRegionsResponses[keyof GetCountryListAllRegionsResponses];

export type GetCountryRetrieveRegionData = {
    body?: never;
    headers: {
        /**
         * List of language codes acceptable for the response. You can specify factors that indicate which language should be retrieved if the one with a higher factor was not found in the localized fields.
         * * If the header is set to a particular language or a list of languages, all localized fields are retrieved as maps with the first matched language only.
         * * If the header is set to `*`, all localized fields are retrieved as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the header is empty, localized fields are retrieved in the default language defined in the Configuration Service.
         *
         */
        'Accept-Language'?: string;
        /**
         * The X-Version header with value v2 is required for the endpoint.
         */
        'X-Version': string;
    };
    path: {
        /**
         * The tenant that the caller is acting upon.
         *
         * Please note that this value is always lowercase.
         *
         */
        tenant: string;
        /**
         * The region code.
         */
        regionCode: string;
    };
    query?: never;
    url: '/country/{tenant}/regions/{regionCode}';
};

export type GetCountryRetrieveRegionErrors = {
    /**
     * Request was syntactically incorrect. Details are provided in the response payload.
     */
    400: ErrorMessage;
    /**
     * The requested resource does not exist.
     */
    404: ErrorMessage;
    /**
     * Internal Service Error occurred.
     */
    500: ErrorMessage;
};

export type GetCountryRetrieveRegionError = GetCountryRetrieveRegionErrors[keyof GetCountryRetrieveRegionErrors];

export type GetCountryRetrieveRegionResponses = {
    /**
     * The request was successful. The requested region is returned.
     */
    200: Region;
};

export type GetCountryRetrieveRegionResponse = GetCountryRetrieveRegionResponses[keyof GetCountryRetrieveRegionResponses];

export type ClientOptions = {
    baseUrl: 'https://api.emporix.io' | (string & {});
};