// AUTO-GENERATED — do not edit
// This file is auto-generated by @hey-api/openapi-ts

/**
 * Localized value which can have different structure which depends on provided `Content-Language` header.
 */
export type LocalizedValue = string | {
    [key: string]: string;
};

export type CoreErrorMessage = {
    /**
     * HTTP Status Code.
     */
    code?: number;
    /**
     * HTTP Status.
     */
    status?: string;
    /**
     * Error message.
     */
    message?: string;
};

export type CoreErrorMessageWithDetails = CoreErrorMessage & {
    /**
     * Error message details.
     */
    details?: Array<{
        field?: string;
        type?: string;
        message?: string;
        moreInfo?: string;
    }>;
};

export type ProductErrorMessage = CoreErrorMessage & {
    /**
     * Identifier of the product.
     */
    productId?: string;
};

export type ProductTemplateBase = {
    /**
     * Localized product template name in a form of a map of translations.
     */
    name: {
        [key: string]: string;
    };
    /**
     * List of attributes related to the product template.
     */
    attributes: Array<ProductTemplateAttribute>;
};

export type ProductTemplateCreation = ProductTemplateBase & {
    /**
     * Identifier of the product template.
     */
    id?: string;
};

export type ProductTemplateAttribute = {
    /**
     * An identifier of the attribute. The key is used as a field name in a product instance created based on the product template. The key can contain only alphanumeric characters.
     */
    key: string;
    /**
     * Localized resource name in a form of a map of translations.
     */
    name: {
        [key: string]: string;
    };
    /**
     * Attribute type. Correct values: `TEXT`, `NUMBER`, `BOOLEAN`, `DATETIME`. Regardless of the chosen type, the defaultValue and values fields are validated. The DATETIME values must be compliant with the ISO 8601 standard, for example: 2022-03-31T09:52:15.423Z.
     */
    type: string;
    /**
     * Metadata contains additional information about the attribute.
     */
    metadata: {
        /**
         * Indicates whether the attribute should be treated as a mandatory.
         */
        mandatory?: boolean;
        /**
         * Indicates whether the attribute is a variant-specific attribute. Variant attributes are taken into account during variants creation.
         * If the value is set to true, then the 'values' array cannot be null.
         */
        variantAttribute?: boolean;
        /**
         * The attribute stores a default value for the attribute.
         */
        defaultValue?: string | boolean | number;
    };
    values?: Values & {
        [key: string]: unknown;
    };
};

export type ProductTemplateMetadata = {
    /**
     * Schema url of the variant attributes.
     */
    variantAttributesSchema?: string;
    /**
     * Schema url of the template attributes.
     */
    templateAttributesSchema?: string;
    /**
     * Date and time when the object was created, compliant with the ISO 8601 standard. For example: `2022-03-31T09:52:15.423Z`
     */
    createdAt?: string;
    /**
     * Date and time when the object was last modified, compliant with the ISO 8601 standard. For example: `2022-03-31T09:52:15.423Z`
     */
    modifiedAt?: string;
    /**
     * Version of the object. The value is required during update operation.
     */
    version?: number;
};

export type ProductTemplateResponse = ProductTemplateCreation & {
    metadata?: ProductTemplateMetadata;
};

export type ProductTemplatesResponse = Array<ProductTemplateResponse>;

export type ProductTemplateUpdate = ProductTemplateBase & {
    metadata: {
        /**
         * Version of the object. The value is required while product template update.
         */
        version: number;
    };
};

/**
 * Indicates the product type, which is immutable. Once the product type is set, it cannot be updated.
 *
 */
export type ProductType = 'BASIC' | 'BUNDLE' | 'PARENT_VARIANT' | 'VARIANT' | 'DYNAMIC_VARIANT';

/**
 * Indicates the product type, which is immutable. This property is taken into account only for insert operations.
 *
 */
export type ProductTypeUpdate = 'BASIC' | 'BUNDLE' | 'PARENT_VARIANT' | 'VARIANT' | 'DYNAMIC_VARIANT';

/**
 * Indicates the template that has been used during the product creation. After assigning a template to the product, the new attributes can be defined under the `productTemplateAttributes` key in the `mixins` field. In case of variant attributes, the attributes should be defined under `productVariantAttributes`. The attributes are then validated in accordance with their definition in the template.
 *
 * **Note:** The template with defined variant attributes can be used only in PARENT_VARIANT and VARIANT types of product.
 *
 */
export type Template = {
    /**
     * Identifier of the product template.
     */
    id: string;
    /**
     * Product template version.
     */
    version: string;
};

/**
 * List of bundled products
 */
export type BundledProducts = Array<{
    /**
     * Identifier of the bundled product.
     */
    productId: string;
    /**
     * Amount of bundled products. Must be a positive number.
     */
    amount: number;
}>;

export type Value = {
    key?: string | number | boolean;
};

export type Values = Array<Value>;

/**
 * This field contains all information about attributes and their values that are used for creating variants. It is presented in the form of a map, where 'key' is the name of the attribute and 'value' is the list of values of that attribute. The attributes and their values are based on their definitions in the `template` element assigned to the parent variant. Each attribute defined in the product template is flagged as a `variantAttribute`. Only the attributes that have this flag set as `true` can be used in the `variantAttributes` field.
 */
export type VariantAttributes = {
    [key: string]: Values;
};

export type ResourceLocation = {
    id?: string;
    yrn?: Yrn;
};

/**
 * In YaaS, a global resource identifier is a Uniform Resource Name (URN) with a custom YaaS schema and is called a YaaS Resource Name, or YRN.
 *
 * It is a unique identifier, which stores information about a resource, such as its type, ID or name of the tenant.
 */
export type Yrn = string;

/**
 * Map of key-value pairs that associates tax classes with locations (countries). The key of the map should be specified as the existing location/country identifier (see the country-service). The value of the map entry should contain a tax class code (see the tax-service).
 * Information about the association is required for price matching mechanism (see the price-service#MatchPrice).
 */
export type TaxClasses = {
    [key: string]: string;
};

/**
 * Flag indicating whether the product has been published or not.
 */
export type Published = boolean;

export type SalePricesData = Array<{
    /**
     * The sale price for the product.
     */
    salePricesAmount?: number;
    /**
     * The start date of the sale price.
     */
    salePriceStart?: string;
    /**
     * The end date of the sale price.
     */
    salePriceEnd?: string;
    /**
     * Marks the enabled sale price. Only one price should be marked.
     */
    enabled?: boolean;
}>;

/**
 * Mixins of the `productCustomAttributes`.
 */
export type ProductCustomAttributes = {
    /**
     * Product price per specified order unit.
     */
    pricingMeasurePrice?: number;
    /**
     * Information about the value and measurement unit per one order unit.
     */
    unitPricingMeasure?: {
        /**
         * Quantity of the measurement unit.
         *
         * For example, if one piece weighs around 180 g, the value should be set to `180`.
         */
        value?: number;
        /**
         * Code of the measurement unit. The possible values for the `unitCode` field are explained in the table below:
         * |     |     |
         * | --- | --- |
         * | **Value** | **Measurement unit** |
         * | `H87` | Pieces |
         * | `KGM` | Kilograms |
         * | `GRM` | Grams |
         * | `LTR` | Liters |
         * | `MLT` | Milliliters |
         * | `MTR` | Meters |
         * | `RO` | Rolls |
         *
         * For example, if one piece weighs around 180 g, the value should be set to `GRM`.
         */
        unitCode?: string;
    };
    /**
     * Information about the value and measurement unit of the base price.
     */
    unitPricingBaseMeasure?: {
        /**
         * Quantity of the base price measurement unit.
         *
         * For example, if the base price is calculated for 1 kg, the value should be set to `1`.
         */
        value?: number;
        /**
         * Code of the measurement unit. The possible values for the `unitCode` field are explained in the table below:
         * |     |     |
         * | --- | --- |
         * | **Value** | **Measurement unit** |
         * | `H87` | Pieces |
         * | `KGM` | Kilograms |
         * | `GRM` | Grams |
         * | `LTR` | Liters |
         * | `MLT` | Milliliters |
         * | `MTR` | Meters |
         * | `RO` | Rolls |
         *
         * For example, if the base price is calculated per 1 kg, the value should be set to `KGM`.
         */
        unitCode?: string;
    };
    /**
     * Information about the product price per specified measurement unit.
     */
    pricingMeasure?: {
        /**
         * Quantity of the price measurement unit.
         *
         * For example, if the price is calculated for 100 g, the value should be set to `100`.
         */
        value?: number;
        /**
         * Code of the measurement unit. The possible values for the `unitCode` field are explained in the table below:
         * |     |     |
         * | --- | --- |
         * | **Value** | **Measurement unit** |
         * | `H87` | Pieces |
         * | `KGM` | Kilograms |
         * | `GRM` | Grams |
         * | `LTR` | Liters |
         * | `MLT` | Milliliters |
         * | `MTR` | Meters |
         * | `RO` | Rolls |
         *
         * For example, if the price is calculated per 100 g, the value should be set to `GRM`.
         */
        unitCode?: string;
    };
    /**
     * Code of the measurement unit by which the customer can order the product. The possible values for the `unitCode` field are explained in the table below:
     * |     |     |
     * | --- | --- |
     * | **Value** | **Measurement unit** |
     * | `H87` | Pieces |
     * | `KGM` | Kilograms |
     * | `GRM` | Grams |
     * | `LTR` | Liters |
     * | `MLT` | Milliliters |
     * | `MTR` | Meters |
     * | `RO` | Rolls |
     *
     * For example, if the customer can order a product by pieces, the value should be set to `H87`.
     */
    orderUnit?: string;
    /**
     * Minimum quantity which can be ordered.
     */
    minOrderQuantity?: number;
    /**
     * Maximum quantity which can be ordered.
     */
    maxOrderQuantity?: number;
    /**
     * Default order quantity which appears on the product page.
     */
    defaultOrderQuantity?: number;
};

/**
 * Mixins of the `productTemplateAttributes` which contains information about the attributes used from the product template in the form of a map, where 'key' is an attribute name and 'value' is the attribute value.
 */
export type ProductTemplateAttributes = {
    [key: string]: unknown;
};

/**
 * Mixins of the `productVariantAttributes` which contains information about the variant attributes used from the product template in the form of a map, where 'key' is an attribute name and 'value' is the attribute value.
 */
export type ProductVariantAttributes = {
    [key: string]: unknown;
};

/**
 * Mixins request.
 */
export type ProductMixins = {
    [key: string]: unknown;
};

/**
 * Mixins request.
 */
export type ProductVariantMixins = {
    [key: string]: unknown;
};

/**
 * Metadata request.
 */
export type ProductMetadataCore = {
    /**
     * Links to the mixin schemas.
     */
    mixins?: {
        [key: string]: unknown;
    };
};

/**
 * Metadata with the version information.
 */
export type ProductMetadataWithVersion = ProductMetadataCore & {
    /**
     * Version of the document
     */
    version?: number;
};

/**
 * Metadata for GET requests.
 */
export type ProductMetadataGet = ProductMetadataWithVersion & {
    /**
     * List of mixin schemas used for classification.
     */
    classificationMixins?: Array<{
        /**
         * Name of the mixin
         */
        name?: string;
        /**
         * The mixins path that should be used when defining attributes on a product. It's built based on the following pattern - `class_<sourceCategory.code>_<name>`.
         */
        mixinPath?: string;
        /**
         * URL of the mixin schema defined on the category level.
         */
        schemaUrl?: string;
        /**
         * If given mixin has been already used in the product, then this field contains the URL of the used mixin schema.
         */
        usedSchemaUrl?: string;
        /**
         * Indicates whether the used mixin schema is obsolete - `usedSchemaUrl` differs from `schemaUrl`.
         */
        obsoleteSchemaUrlUsed?: boolean;
        /**
         * Indicates whether the mixin is required.
         */
        required?: boolean;
        /**
         * Unique identifier of the category from which the mixin comes from. Can be this category ID or one of the parent categories IDs.
         */
        sourceCategoryId?: string;
    }>;
    /**
     * Creation date in the ISO 8601 standard.
     */
    createdAt?: string;
    /**
     * Modification date in the ISO 8601 standard.
     */
    modifiedAt?: string;
};

/**
 * Variant metadata response.
 */
export type VariantProductMetadataGet = ProductMetadataGet & VariantProductMetadataWithVersion;

/**
 * Metadata request.
 */
export type VariantProductMetadataCore = ProductMetadataCore & {
    /**
     * By default, all variant products inherit their attributes from the parent variant. You need to include the name of the attribute in the `metadata.overridden` field to be able to replace the attribute values. For the `mixins` and `metadata` fields, you can provide a nested path so that only a single mixin attribute is overridden, for example `mixins.productCustomAttributes.pricingMeasurePrice`. The same rule is valid for providing the mixins schema, as you can override just this particular field with the provided schema, for example metadata.mixins.yourCustomAttributes.
     *
     * **Note:**You cannot override the following attributes: `id`, `code`, `template`, `variantAttributes`.
     */
    overridden?: Array<string>;
};

/**
 * Metadata with the version information.
 */
export type VariantProductMetadataWithVersion = VariantProductMetadataCore & {
    /**
     * Version of the document.
     */
    version?: number;
};

/**
 * Unique resource identifier.
 */
export type Id = string;

/**
 * Media files attached to the product.
 */
export type ProductMedia = Array<{
    id?: string;
    yrn?: Yrn;
    contentType?: string;
    createdAt?: string;
    uploadLink?: string;
    url?: string;
    cloudinaryUrl?: string;
    uncommittedMedia?: boolean;
    tags?: Array<string>;
    customAttributes?: {
        [key: string]: unknown;
    };
    /**
     * Custom media attributes.
     */
    mixins?: {
        [key: string]: unknown;
    };
}>;

export type ProductCore = {
    name?: LocalizedValue;
    /**
     * Unique product identifier, defined by the user.
     */
    code?: string;
    description?: LocalizedValue;
    vendor?: Vendor;
    taxClasses?: TaxClasses;
    published?: Published;
    /**
     * Mark the product as weight-dependent if the line item price could change after packaging, as this is necessary to ensure that credit card payments can be authorized.
     */
    weightDependent?: boolean;
    mixins?: ProductMixins;
    /**
     * List of items in a relationship with the main product. Assuming that in the system the following relation types are defined: `ACCESSORY` and `CONSUMABLE`, the two products: `Printer` and `Ink` exist, then the `Ink` product can be used as a related item with type `CONSUMABLE` for the Printer product.
     * In order to find all printers for which the ink is defined as a `CONSUMABLE` then `q-param` can be used:
     * `products?q=relatedItems.type:CONSUMABLE relatedItems.refId:Ink_Id`
     *
     */
    relatedItems?: Array<RelatedItem>;
    /**
     * ID of brand.
     */
    brandId?: string;
    brand?: Brand;
    /**
     * Collection of label IDs.
     */
    labelIds?: Array<string>;
    labels?: Array<Label>;
};

/**
 * Brand data expanded from `brandId`.
 */
export type Brand = {
    id?: string;
    name?: string;
    description?: string;
    localizedName?: {
        [key: string]: string;
    };
    localizedDescription?: {
        [key: string]: string;
    };
    image?: string;
    cloudinaryUrl?: string;
    mediaId?: string;
};

/**
 * Overlay metadata for a label.
 */
export type LabelOverlay = {
    isTrue?: boolean;
    position?: number;
};

/**
 * Label data expanded from `labelIds`.
 */
export type Label = {
    id?: string;
    name?: string;
    description?: LocalizedValue;
    image?: string;
    cloudinaryUrl?: string;
    mediaId?: string;
    overlay?: LabelOverlay;
};

/**
 * Products of the BASIC type.
 */
export type BasicProduct = ProductCore & {
    metadata?: ProductMetadataCore;
    template?: Template;
};

/**
 * Creating products of the BASIC type.
 */
export type BasicProductCreation = BasicProduct & {
    /**
     * Custom product identifier. If not provided, it is automatically generated.
     */
    id?: string;
    productType?: ProductType;
};

/**
 * Products of the BASIC type.
 */
export type BasicProductWithId = BasicProductCreation & {
    id?: Id;
    yrn?: Yrn;
    media?: ProductMedia;
    metadata?: ProductMetadataGet;
    /**
     * Customer segment IDs assigned to the product.
     */
    customerSegmentIds?: Array<string>;
    /**
     * Category IDs assigned to the product.
     */
    categoryIds?: Array<string>;
};

/**
 * List of products of the BASIC type.
 */
export type BasicProductsArray = Array<BasicProductWithId>;

/**
 * Updating products of the BASIC type.
 */
export type BasicProductUpdate = BasicProduct & {
    metadata?: ProductMetadataWithVersion;
    productType?: ProductTypeUpdate;
};

/**
 * Updating products of the BASIC type in bulk.
 */
export type BasicProductBulkUpdate = BasicProductUpdate & {
    id: Id;
};

/**
 * Products of the BUNDLE type.
 */
export type BundleProduct = ProductCore & {
    metadata?: ProductMetadataCore;
    template?: Template;
    bundledProducts: BundledProducts;
    productType: ProductTypeUpdate;
};

/**
 * Creating products of the BUNDLE type.
 */
export type BundleProductCreation = BundleProduct & {
    /**
     * Custom product identifier. If not provided, it is automatically generated.
     */
    id?: string;
    productType?: ProductType;
};

/**
 * Products of the BUNDLE type.
 */
export type BundleProductWithId = BundleProductCreation & {
    id?: Id;
    yrn?: Yrn;
    media?: ProductMedia;
    metadata?: ProductMetadataGet;
    /**
     * Customer segment IDs assigned to the product.
     */
    customerSegmentIds?: Array<string>;
    /**
     * Category IDs assigned to the product.
     */
    categoryIds?: Array<string>;
};

/**
 * List of products of the BUNDLE type.
 */
export type BundleProductsArray = Array<BundleProductWithId>;

/**
 * Updating products of the BUNDLE type.
 */
export type BundleProductUpdate = BundleProduct & {
    metadata?: ProductMetadataWithVersion;
};

/**
 * Updating products of the BUNDLE type in bulk.
 */
export type BundleProductBulkUpdate = BundleProductUpdate & {
    id: Id;
};

/**
 * Products of the PARENT_VARIANT type.
 */
export type ParentVariantProduct = ProductCore & {
    metadata?: ProductMetadataCore;
    template: Template;
    variantAttributes: VariantAttributes;
};

/**
 * Creating products of the PARENT_VARIANT type.
 */
export type ParentVariantProductCreation = ParentVariantProduct & {
    /**
     * Custom product identifier. If not provided, it is automatically generated.
     */
    id?: string;
    productType?: ProductType;
};

/**
 * Products of the PARENT_VARIANT type.
 */
export type ParentVariantProductWithId = ParentVariantProductCreation & {
    id?: Id;
    yrn?: Yrn;
    media?: ProductMedia;
    metadata?: ProductMetadataGet;
    /**
     * Customer segment IDs assigned to the product.
     */
    customerSegmentIds?: Array<string>;
    /**
     * Category IDs assigned to the product.
     */
    categoryIds?: Array<string>;
};

/**
 * List of products of the PARENT_VARIANT type.
 */
export type ParentVariantProductsArray = Array<ParentVariantProductWithId>;

/**
 * Updating products of the PARENT_VARIANT type.
 */
export type ParentVariantProductUpdate = ParentVariantProduct & {
    metadata?: ProductMetadataWithVersion;
    productType?: ProductTypeUpdate;
};

/**
 * Updating products of the PARENT_VARIANT type in bulk.
 */
export type ParentVariantProductBulkUpdate = ParentVariantProductUpdate & {
    id: Id;
};

/**
 * Products of the VARIANT type.
 */
export type VariantProduct = ProductCore & {
    mixins?: ProductVariantMixins;
};

/**
 * Creating products of the VARIANT type.
 */
export type VariantProductCreation = VariantProduct & {
    /**
     * Custom product identifier. If not provided, it is automatically generated.
     */
    id?: string;
    metadata?: VariantProductMetadataCore;
    /**
     * Identifier of the PARENT_VARIANT product.
     */
    parentVariantId?: string;
    productType?: ProductType;
};

/**
 * Products of the VARIANT type.
 */
export type VariantProductWithId = VariantProductCreation & {
    id?: Id;
    yrn?: Yrn;
    media?: ProductMedia;
    metadata?: VariantProductMetadataGet;
    /**
     * Customer segment IDs assigned to the product.
     */
    customerSegmentIds?: Array<string>;
    /**
     * Category IDs assigned to the product.
     */
    categoryIds?: Array<string>;
};

/**
 * List of products of the VARIANT type.
 */
export type VariantProductsArray = Array<VariantProductWithId>;

/**
 * Updating products of the VARIANT type.
 */
export type VariantProductUpdate = VariantProduct & {
    metadata?: VariantProductMetadataWithVersion;
    productType?: ProductTypeUpdate;
    /**
     * Identifier of the PARENT_VARIANT product. This property is taken into account only for insert operations.
     */
    parentVariantId?: string;
};

/**
 * Updating products of the VARIANT type in bulk.
 */
export type VariantProductBulkUpdate = VariantProductUpdate & {
    id: Id;
};

/**
 * The value part of a dynamic variant attribute. Holds the canonical qualifier used for matching, an optional localized display label, and an optional unit code for measurement attributes.
 *
 */
export type DynamicVariantAttributeValue = {
    /**
     * Data type of the qualifier value.
     *
     */
    type: 'STRING' | 'NUMBER' | 'DECIMAL' | 'BOOLEAN';
    /**
     * The canonical value used for variant matching. The JSON type must match `type`: a string for `STRING`, a number for `NUMBER` or `DECIMAL`, or a boolean for `BOOLEAN`.
     *
     */
    qualifier: string | number | boolean;
    name?: LocalizedValue;
    /**
     * Optional unit code for measurement attributes (for example, `"MMT"` for millimetres, `"PCE"` for pieces). Unit translations are resolved by the frontend from a separate dictionary.
     *
     */
    unit?: string;
};

/**
 * A single dynamic variant attribute. Combines a localized display name for the attribute itself with a typed, optionally localized value.
 *
 */
export type DynamicVariantAttribute = {
    name?: LocalizedValue;
    value: DynamicVariantAttributeValue;
};

/**
 * Map of variant attributes defined directly on this product. Keys are attribute identifiers (for example, `storageCapacity`). These are the attributes the caller can set by create/update requests. On responses they represent what is stored on this specific product.
 *
 */
export type OwnVariantAttributes = {
    [key: string]: DynamicVariantAttribute;
};

/**
 * Read-only map of variant attributes inherited from ancestor products in the hierarchy. These cannot be modified on this product — to change an inherited attribute, update the ancestor that owns it.
 *
 */
export type InheritedVariantAttributes = {
    [key: string]: DynamicVariantAttribute;
};

/**
 * A single entry in the root product's `variants` map. In API responses the `variantAttributes` field contains **accumulated** attributes — the entry's own delta attributes merged with all ancestor attributes up the chain, so storefront clients receive a ready-to-use combination without additional API calls. The `published` flag is included only when the caller has the `product.product_read_unpublished` scope.
 *
 */
export type VariantMapEntry = {
    /**
     * Version of the source variant product at the time of the last recalculation.
     */
    version?: number;
    /**
     * Code of the source variant product.
     */
    code?: string;
    name?: LocalizedValue;
    /**
     * Identifier of the immediate parent product in the variant hierarchy.
     */
    parentVariantId?: string;
    /**
     * Indicates whether this variant can be sold directly.
     */
    sellable?: boolean;
    /**
     * Publication state of this variant. Only included in responses when the caller has the `product.product_read_unpublished` scope; entries with `published: false` are otherwise omitted from the map.
     *
     */
    published?: boolean;
    /**
     * Optional label identifying the variant level or kind (for example, `"H1_L1"`).
     */
    dynamicVariantType?: string;
    /**
     * Accumulated variant attributes for this entry. For leaf variants this includes all ancestor attributes merged with the entry's own delta. Keys are attribute identifiers.
     *
     */
    variantAttributes?: {
        [key: string]: DynamicVariantAttribute;
    };
};

/**
 * Denormalized flat map of all descendant variants stored on the root product. Keys are variant product identifiers. In API responses each entry's `variantAttributes` contains accumulated attributes.
 *
 */
export type VariantsMap = {
    [key: string]: VariantMapEntry;
};

/**
 * Hierarchy integrity information populated when the variant tree has structural problems. Present only when an issue is detected.
 *
 */
export type DynamicVariantInfo = {
    /**
     * Identifier of an ancestor product that could not be found, indicating a broken hierarchy chain.
     */
    missingAncestorId?: string;
    /**
     * Indicates that a circular reference was detected in the `parentVariantId` chain.
     */
    cycleDetected?: boolean;
};

/**
 * Metadata for GET responses on DYNAMIC_VARIANT products. Extends the standard product metadata with hierarchy integrity information.
 */
export type DynamicVariantProductMetadataGet = ProductMetadataGet & {
    dynamicVariantInfo?: DynamicVariantInfo;
};

/**
 * Base schema for products of the DYNAMIC_VARIANT type.
 */
export type DynamicVariantProduct = ProductCore & {
    metadata?: ProductMetadataCore;
    /**
     * Identifier of the immediate parent product in the variant hierarchy. Omit or leave empty for root-level dynamic variant products.
     *
     */
    parentVariantId?: string;
    /**
     * Optional label identifying the variant level or kind within the hierarchy (for example, `"H1_L1"`). Used for display and filtering purposes.
     *
     */
    dynamicVariantType?: string;
    /**
     * Indicates whether this variant can be sold directly, if it's a leaf in the hierarchy that a customer can add to a cart. This value is **not inherited** from parent variants — it must be set explicitly on each product. Root products and intermediate-level variants are typically `false`. Leaf variants are typically `true`.
     *
     */
    sellable?: boolean;
    ownVariantAttributes?: OwnVariantAttributes;
};

/**
 * Creating products of the DYNAMIC_VARIANT type.
 */
export type DynamicVariantProductCreation = DynamicVariantProduct & {
    /**
     * Custom product identifier. If not provided, it is automatically generated.
     */
    id?: string;
    productType?: ProductType;
};

/**
 * Response schema for products of the DYNAMIC_VARIANT type.
 *
 * - **`sellable`** — not inherited from parents; must be set explicitly on each product. Root and intermediate variants are typically `false`; leaf variants are typically `true`.
 * - **Root products** include a `variants` map containing all descendants with accumulated attributes.
 * - **Child products** include `ownVariantAttributes` (attributes defined on this product) and `inheritedVariantAttributes` (read-only, merged from ancestors).
 * - `parentVariantPath` is an ordered array of ancestor IDs from direct parent (index 0) to root (last index). Empty for root products.
 * - `metadata.dynamicVariantInfo` is present only when a hierarchy integrity issue is detected.
 *
 */
export type DynamicVariantProductWithId = {
    id: Id;
    yrn?: Yrn;
    /**
     * Unique product identifier, defined by the user.
     */
    code: string;
    name: LocalizedValue;
    description?: LocalizedValue;
    productType: ProductType;
    published?: Published;
    /**
     * Indicates whether this variant can be sold directly (i.e. it is a leaf in the hierarchy that a customer can add to a cart). This value is **not inherited** from parent variants — it must be set explicitly on each product. Root products and intermediate-level variants are typically `false`; leaf variants are typically `true`.
     *
     */
    sellable?: boolean;
    /**
     * Identifier of the immediate parent product in the variant hierarchy. Absent for root-level products.
     */
    parentVariantId?: string;
    /**
     * Ordered array of ancestor product identifiers, from the direct parent (index 0) to the root (last index). Empty for root-level products.
     *
     */
    parentVariantPath?: Array<string>;
    /**
     * Optional label identifying the variant level or kind within the hierarchy (for example, `"H1_L1"`).
     */
    dynamicVariantType?: string;
    ownVariantAttributes?: OwnVariantAttributes;
    inheritedVariantAttributes?: InheritedVariantAttributes;
    variants?: VariantsMap;
    taxClasses?: TaxClasses;
    vendor?: Vendor;
    /**
     * ID of brand.
     */
    brandId?: string;
    brand?: Brand;
    /**
     * Collection of label IDs.
     */
    labelIds?: Array<string>;
    labels?: Array<Label>;
    /**
     * Category IDs assigned to the product.
     */
    categoryIds?: Array<string>;
    /**
     * Customer segment IDs assigned to the product.
     */
    customerSegmentIds?: Array<string>;
    /**
     * List of items in a relationship with this product.
     */
    relatedItems?: Array<RelatedItem>;
    /**
     * Mark the product as weight-dependent if the line item price could change after packaging.
     */
    weightDependent?: boolean;
    mixins?: ProductMixins;
    media?: ProductMedia;
    metadata?: DynamicVariantProductMetadataGet;
};

/**
 * List of products of the DYNAMIC_VARIANT type.
 */
export type DynamicVariantProductsArray = Array<DynamicVariantProductWithId>;

/**
 * Updating products of the DYNAMIC_VARIANT type.
 */
export type DynamicVariantProductUpdate = DynamicVariantProduct & {
    metadata?: ProductMetadataWithVersion;
    productType?: ProductTypeUpdate;
};

/**
 * Updating products of the DYNAMIC_VARIANT type in bulk.
 */
export type DynamicVariantProductBulkUpdate = DynamicVariantProductUpdate & {
    id: Id;
};

/**
 * Request body for triggering dynamic variant recalculation. Accepts up to 1000 product IDs at any level of the hierarchy — the system resolves the root for each ID automatically. Intended to be called after a batch of products has been fully ingested, not per individual product write.
 *
 */
export type DynamicVariantRecalculationRequest = {
    /**
     * List of product IDs (variants at any hierarchy level) for which recalculation should be triggered. The system groups IDs by their root product and creates one job per unique root.
     *
     */
    productIds: Array<string>;
};

/**
 * Current status of a dynamic variant recalculation job.
 */
export type DynamicVariantRecalculationJobStatus = 'PENDING' | 'PROCESSING' | 'FINISHED' | 'FAILED' | 'FAILED_PERMANENT';

/**
 * Represents a single dynamic variant recalculation job. One job is created per unique root product. Jobs remain queryable until automatically removed approximately 30 days after reaching a terminal state.
 *
 */
export type DynamicVariantRecalculationJobResponse = {
    /**
     * Unique identifier of the recalculation job.
     */
    id?: string;
    /**
     * Identifier of the root product whose variant tree is being recalculated.
     */
    parentRoot?: string;
    /**
     * List of product IDs that were submitted in the recalculation request and resolved to this root.
     */
    requestedByIds?: Array<string>;
    status?: DynamicVariantRecalculationJobStatus;
    /**
     * Timestamp when the job was created.
     */
    createdAt?: string;
    /**
     * Timestamp when a subscriber picked up the job and started processing. Null while the job is still PENDING.
     */
    processingStartedAt?: string;
    /**
     * Timestamp when the job reached a terminal state (FINISHED, FAILED, or FAILED_PERMANENT).
     */
    finishedAt?: string;
    /**
     * Error message if the job failed. Null for successful jobs.
     */
    error?: string;
    /**
     * Number of processing attempts made for this job. Incremented on each delivery from the pub/sub subscription.
     */
    attempts?: number;
};

/**
 * Response returned when recalculation is triggered. Contains one job per unique root product resolved from the submitted IDs, plus any IDs that were skipped (for example, because a job for that root is already PENDING or PROCESSING).
 *
 */
export type DynamicVariantRecalculationResponse = {
    /**
     * List of recalculation jobs created or referenced for each unique root product.
     */
    jobs?: Array<DynamicVariantRecalculationJobResponse>;
    /**
     * Product IDs that were not processed because a recalculation job for their root is already active (PENDING or PROCESSING). The existing job ID is available in the `jobs` list.
     *
     */
    skippedProductIds?: Array<string>;
};

/**
 * Partially updating a product.
 */
export type ProductPartialUpdate = ProductCore & {
    template?: Template;
    bundledProducts?: BundledProducts;
    variantAttributes?: VariantAttributes;
    metadata?: ProductMetadataWithVersion;
};

export type Vendor = {
    /**
     * An identifier of a vendor to whom the product belongs.
     */
    id: string;
    /**
     * Vendor name.
     */
    name?: string;
};

export type SearchProducts = {
    /**
     * A standard query parameter is used to search for specific values.
     *
     * See: [Standard practises - Query parameter](https://developer.emporix.io/api-references/standard-practices/q-param)
     *
     */
    q?: string;
};

export type SearchQuery = {
    /**
     * List of YRNs. Each YRN identifies a separate product.
     */
    yrns: Array<Yrn>;
    /**
     * Optional query parameters.
     */
    params?: {
        /**
         * Optional query parameters for products.
         */
        product?: {
            /**
             * Optional projection for product.
             */
            fields?: Array<string>;
        };
    };
};

export type RelatedItem = {
    /**
     * Identifier of a product which is in the relationship with the main product. The product identifier has to exist.
     */
    refId: string;
    /**
     * A relation type. The type has to exist in the system. All relation types are defined in configuration service under `relation_types` key.
     */
    type: string;
};

export type BulkResponse = {
    /**
     * Index of the processed product, matching the price position in request body.
     *
     */
    index?: number;
    /**
     * Product unique identifier.
     */
    id?: string;
    /**
     * HTTP Status Code.
     */
    code?: number;
    /**
     * Error specific errorCode.
     */
    errorCode?: string;
    /**
     * HTTP Status description.
     */
    status?: string;
    /**
     * Error message.
     */
    message?: string;
    /**
     * Error message details.
     */
    details?: Array<string>;
};

/**
 * Your Emporix tenant name.
 *
 * **Note**: The tenant should always be written in lowercase.
 *
 */
export type PathTenant = string;

/**
 * The page number to be retrieved where the size of the pages must be specified by the pageSize parameter. The number of the first page is 1.
 */
export type QueryPageNumber = number;

/**
 * The number of documents being retrieved on the page.
 */
export type QueryPageSize = number;

/**
 * Fields to sort the response data by following order of the parameters from left to right.
 * Can contain multiple fields in format: `field name:sort direction` separated by a comma.
 * The colon with `sort direction` parameter is redundant and descending order is taken only if it is equal to `desc` or `DESC`.
 * Ascending order is considered in any other case.
 * While sorting by a localised field the following rule is taken into account:
 * 1. If a localized field contains information about a language, then the language should be used. For example `name.en:ASC`. In that case en language is used for sorting the name property. The language suffix takes precedence over `Accept-Language` header.
 * 2. If a localized field does not contain information about a language, then the `Accept-Language` header is used. For example `name:ASC` and `Accept-Language: de`. In that case `de` is used for sorting the name property.
 * 3. If a localized field does not contain information about language and `Accept-Language` header contains `*`, then the default language from configuration service is used as a language. For example `name:ASC` and `Accept-Language: *` and default language is pl then pl is used as a language for name property.
 * 4. If a localized field does not contains information about language and `Accept-Language` header does not exists in the request, then the default language from configuration service is used.  "
 *
 */
export type QuerySort = string;

/**
 * The Content-Language request HTTP header defines a language or multiple languages of the request body.
 *
 * * If the `Content-Language` header is set to `*`, the localized fields should be provided as maps of translations, where the keys are language codes and values are the fields in their respective languages.
 * * If the `Content-Language` header is set to a specific language, the localized fields should be provided as strings.
 * * If the `Content-Language` header is empty, the endpoint interprets the localized fields as being provided in the default language or the languages defined in the Configuration Service.
 *
 * **Note**: You can provide the localized fields only in languages defined in the Configuration Service. In case the fields are provided in languages that are not defined in the Configuration Service, the request is rejected.
 *
 */
export type HeaderContentLanguage = string;

/**
 * The Accept-Language request HTTP header defines which languages the client is able to understand, and which locale variant is preferred. If empty, the default system language is assumed. It can be a priority list working as a fallback mechanism.
 */
export type HeaderAcceptLanguage = string;

/**
 * Flag indicating whether the total number of retrieved results should be returned.
 */
export type HeaderXTotalCount = boolean;

/**
 * Product unique identifier generated when the product is created.
 */
export type PathProductId = string;

/**
 * A standard query parameter is used to search for specific values.
 *
 * See: [Standard practises - Query parameter](https://developer.emporix.io/api-references/standard-practices/q-param)
 *
 */
export type QueryQProduct = string;

/**
 * List of properties used to sort the results, separated by colons.
 *
 * Possible values:
 * * `{fieldName}`
 * * `{fieldName}:asc`
 * * `{fieldName}:desc`
 *
 * **Note:** If you want to sort the results by localized properties, the possible values are:
 * * `{fieldName}.{language}`
 * * `{fieldName}.{language}:asc`
 * * `{fieldName}.{language}:desc`
 *
 * By default, the fields are sorted in ascending order.
 *
 */
export type QuerySortProduct = string;

/**
 * | Option  | Description |
 * |---|---|
 * | `true` | A partial product update is performed. |
 * | `false` | A full product replacement is performed.|
 *
 */
export type QueryPartialProduct = boolean;

/**
 * Flag indicating whether product variant(s) should include their parent variants' attribute values.
 * * If set to `true`, the result contains values from both the variant and its parent variant.
 * * If set to `false`, the result contains only variant values.
 *
 */
export type QueryRawValueProduct = boolean;

/**
 * Fields that should be expanded with additional information in the response body. Expressed as strings separated by commas.
 *
 * Possible values:
 * * `template`
 *
 */
export type QueryExpandSearch = string;

/**
 * Fields that should be expanded with additional information in the response body. Expressed as strings separated by commas. You can combine multiple values, for example `expand=template,parentVariant,brand,labels`.
 *
 * Possible values:
 * * `template`
 * * `parentVariant`
 * * `brand`
 * * `labels`
 *
 */
export type QueryExpandProduct = string;

/**
 * Removing a parent variant product also removes all of its child variants. This applies to both `PARENT_VARIANT` roots with `VARIANT` children and `DYNAMIC_VARIANT` parent products with all descendant dynamic variants in the tree. This operation has to be confirmed by setting `force=true`.
 *
 */
export type QueryForceProduct = boolean;

/**
 * Unique identifier of the product template.
 */
export type PathProductTemplateId = string;

/**
 * Standard query parameter used to search for specific values.
 * * Searching for an item by string property: `q=id:31065d5b-b62e`, where `id` is the field name and `31065d5b-b62e` is its required value.
 * * Searching for an item by localized field property: `q=name.en:T-s` where `name` is the name of the field, `en` is a language code and `T-s` is a required value of this field.   This query works only for localized fields, which are stored in a Map format where `key` is a language code and `value` is translation to particular language.   + Searching for items by date property. All numer-based property queries are valid also for dates. In that case the date should be placed within double quotes: `q=metadata.createdAt:(>="2021-05-18T07:27:27.455Z" AND <"2021-05-20T07:27:27.455Z")`   + Searching for items with non existing or empty property: `q=name.en:null` where `name.en` is a name of fields that has value `null`.   + Searching for items with existing property: `q=attributes:exists` where `attributes` is a name of field that has `non null` value.   + Searching for items by multiple specific values: `q=id:(5c3325baa9812100098ff48f,5c3325d1a9812100098ff494)` where `id` is name of field and strings within a bracket are it''s required value.   + Searching for items by multiple fields: `q=id:5c3325baa9812100098ff48f name.en:T-s` where `id` and ''name.en'' are the names of fields. All documents that contain given values of these fields are returned. Multiple fields separated by space can be specified. Multiple values for each field can be also specified in a format presented above.   + Searching for items with string fields conforming to a regex: `q=name.en:~ABCD12` or `q=name.en:(~AB CD)` - in case of searching for strings with space, where `name` is the name of field and `ABCD12` or `AB CD` is it''s querying regex.'
 *
 */
export type QueryQParamProductTemplate = string;

/**
 * Version of the product template. If the parameter is not provided, the latest version of product template is returned.
 */
export type QueryVersionParameterProductTemplate = string;

/**
 * Fields to be returned in the response.
 *
 * When this parameter is passed, only the `id`, `yrn` and `{fieldName}` are retrieved for each product.
 *
 * You can specify multiple fields by separating them with commas.
 *
 */
export type QueryProductFields = string;

/**
 * The parameter is valid only for `PARENT_VARIANT` type. If true then variants are not automatically generated for the parent variant configuration.
 */
export type QuerySkipVariantsGeneration = boolean;

/**
 * Allows to control whether the change of the state should cause reindexing or not.
 */
export type QueryDoIndex = boolean;

/**
 * If set to `true`, the validation that checks whether related products exist is skipped. This is useful during data loading, where related products may not yet exist and will be created later. This parameter is only respected when the `allowToSkipRelatedProductsValidation` system setting is set to `true`. Otherwise, it is disregarded.
 */
export type QuerySkipRelatedItemsValidation = boolean;

export type ProductsSearchBody = SearchProducts;

export type ProductCreateBody = BasicProductCreation | BundleProductCreation | ParentVariantProductCreation | VariantProductCreation | DynamicVariantProductCreation;

export type ProductUpdateBody = BasicProductUpdate | BundleProductUpdate | ParentVariantProductUpdate | VariantProductUpdate | DynamicVariantProductUpdate;

export type ProductPartialUpdateBody = ProductPartialUpdate;

export type ProductBulkUpdateBody = Array<BasicProductBulkUpdate | BundleProductBulkUpdate | ParentVariantProductBulkUpdate | VariantProductBulkUpdate | DynamicVariantProductBulkUpdate>;

/**
 * A list of product create requests
 */
export type ProductBulkCreateBody = Array<BasicProductCreation | BundleProductCreation | ParentVariantProductCreation | VariantProductCreation | DynamicVariantProductCreation>;

export type PostProductSearchProductsData = {
    body?: ProductsSearchBody;
    headers?: {
        /**
         * Flag indicating whether the total number of retrieved results should be returned.
         */
        'X-Total-Count'?: boolean;
        /**
         * The Accept-Language request HTTP header defines which languages the client is able to understand, and which locale variant is preferred. If empty, the default system language is assumed. It can be a priority list working as a fallback mechanism.
         */
        'Accept-Language'?: string;
    };
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * The page number to be retrieved where the size of the pages must be specified by the pageSize parameter. The number of the first page is 1.
         */
        pageNumber?: number;
        /**
         * The number of documents being retrieved on the page.
         */
        pageSize?: number;
        /**
         * List of properties used to sort the results, separated by colons.
         *
         * Possible values:
         * * `{fieldName}`
         * * `{fieldName}:asc`
         * * `{fieldName}:desc`
         *
         * **Note:** If you want to sort the results by localized properties, the possible values are:
         * * `{fieldName}.{language}`
         * * `{fieldName}.{language}:asc`
         * * `{fieldName}.{language}:desc`
         *
         * By default, the fields are sorted in ascending order.
         *
         */
        sort?: string;
        /**
         * Fields to be returned in the response.
         *
         * When this parameter is passed, only the `id`, `yrn` and `{fieldName}` are retrieved for each product.
         *
         * You can specify multiple fields by separating them with commas.
         *
         */
        fields?: string;
        /**
         * Fields that should be expanded with additional information in the response body. Expressed as strings separated by commas. You can combine multiple values, for example `expand=template,parentVariant,brand,labels`.
         *
         * Possible values:
         * * `template`
         * * `parentVariant`
         * * `brand`
         * * `labels`
         *
         */
        expand?: string;
        /**
         * Flag indicating whether product variant(s) should include their parent variants' attribute values.
         * * If set to `true`, the result contains values from both the variant and its parent variant.
         * * If set to `false`, the result contains only variant values.
         *
         */
        rawValue?: boolean;
    };
    url: '/product/{tenant}/products/search';
};

export type PostProductSearchProductsErrors = {
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type PostProductSearchProductsError = PostProductSearchProductsErrors[keyof PostProductSearchProductsErrors];

export type PostProductSearchProductsResponses = {
    /**
     * Resources have been retrieved successfully.
     */
    200: BasicProductsArray | BundleProductsArray | ParentVariantProductsArray | VariantProductsArray | DynamicVariantProductsArray;
};

export type PostProductSearchProductsResponse = PostProductSearchProductsResponses[keyof PostProductSearchProductsResponses];

export type GetProductListProductsData = {
    body?: never;
    headers?: {
        /**
         * Flag indicating whether the total number of retrieved results should be returned.
         */
        'X-Total-Count'?: boolean;
        /**
         * The Accept-Language request HTTP header defines which languages the client is able to understand, and which locale variant is preferred. If empty, the default system language is assumed. It can be a priority list working as a fallback mechanism.
         */
        'Accept-Language'?: string;
    };
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * The page number to be retrieved where the size of the pages must be specified by the pageSize parameter. The number of the first page is 1.
         */
        pageNumber?: number;
        /**
         * The number of documents being retrieved on the page.
         */
        pageSize?: number;
        /**
         * List of properties used to sort the results, separated by colons.
         *
         * Possible values:
         * * `{fieldName}`
         * * `{fieldName}:asc`
         * * `{fieldName}:desc`
         *
         * **Note:** If you want to sort the results by localized properties, the possible values are:
         * * `{fieldName}.{language}`
         * * `{fieldName}.{language}:asc`
         * * `{fieldName}.{language}:desc`
         *
         * By default, the fields are sorted in ascending order.
         *
         */
        sort?: string;
        /**
         * A standard query parameter is used to search for specific values.
         *
         * See: [Standard practises - Query parameter](https://developer.emporix.io/api-references/standard-practices/q-param)
         *
         */
        q?: string;
        /**
         * Fields to be returned in the response.
         *
         * When this parameter is passed, only the `id`, `yrn` and `{fieldName}` are retrieved for each product.
         *
         * You can specify multiple fields by separating them with commas.
         *
         */
        fields?: string;
        /**
         * Fields that should be expanded with additional information in the response body. Expressed as strings separated by commas. You can combine multiple values, for example `expand=template,parentVariant,brand,labels`.
         *
         * Possible values:
         * * `template`
         * * `parentVariant`
         * * `brand`
         * * `labels`
         *
         */
        expand?: string;
        /**
         * Flag indicating whether product variant(s) should include their parent variants' attribute values.
         * * If set to `true`, the result contains values from both the variant and its parent variant.
         * * If set to `false`, the result contains only variant values.
         *
         */
        rawValue?: boolean;
    };
    url: '/product/{tenant}/products';
};

export type GetProductListProductsErrors = {
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type GetProductListProductsError = GetProductListProductsErrors[keyof GetProductListProductsErrors];

export type GetProductListProductsResponses = {
    /**
     * Resources have been retrieved successfully.
     */
    200: BasicProductsArray | BundleProductsArray | ParentVariantProductsArray | VariantProductsArray | DynamicVariantProductsArray;
};

export type GetProductListProductsResponse = GetProductListProductsResponses[keyof GetProductListProductsResponses];

export type PostProductCreateProductData = {
    body?: ProductCreateBody;
    headers?: {
        /**
         * The Content-Language request HTTP header defines a language or multiple languages of the request body.
         *
         * * If the `Content-Language` header is set to `*`, the localized fields should be provided as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the `Content-Language` header is set to a specific language, the localized fields should be provided as strings.
         * * If the `Content-Language` header is empty, the endpoint interprets the localized fields as being provided in the default language or the languages defined in the Configuration Service.
         *
         * **Note**: You can provide the localized fields only in languages defined in the Configuration Service. In case the fields are provided in languages that are not defined in the Configuration Service, the request is rejected.
         *
         */
        'Content-Language'?: string;
    };
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * The parameter is valid only for `PARENT_VARIANT` type. If true then variants are not automatically generated for the parent variant configuration.
         */
        skipVariantGeneration?: boolean;
        /**
         * Allows to control whether the change of the state should cause reindexing or not.
         */
        doIndex?: boolean;
        /**
         * If set to `true`, the validation that checks whether related products exist is skipped. This is useful during data loading, where related products may not yet exist and will be created later. This parameter is only respected when the `allowToSkipRelatedProductsValidation` system setting is set to `true`. Otherwise, it is disregarded.
         */
        skipRelatedItemsValidation?: boolean;
    };
    url: '/product/{tenant}/products';
};

export type PostProductCreateProductErrors = {
    /**
     * Resources cannot be created due to an error.
     */
    400: CoreErrorMessage;
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * There are three possible reasons:
     * 1. Product with given code already exists, please choose unique code for your product
     * 2. Optimistic locking failed. If user sends metadata/version attribute which is outdated (someone else updated product in the time user was performing his changes). User should retrieve the latest product data and retry the request.
     * 3. Optimistic locking failed. User did not provide metadata/version attribute in update request, but someone else updated product while it was internally handled by product service. Resending the same request can result in successful update, but the update can override recently persisted changes.
     *
     */
    409: CoreErrorMessageWithDetails;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type PostProductCreateProductError = PostProductCreateProductErrors[keyof PostProductCreateProductErrors];

export type PostProductCreateProductResponses = {
    /**
     * The resource has been successfully created.
     *
     */
    201: ResourceLocation;
};

export type PostProductCreateProductResponse = PostProductCreateProductResponses[keyof PostProductCreateProductResponses];

export type PostProductCreateBulkProductsData = {
    /**
     * A list of product create requests
     */
    body?: ProductBulkCreateBody;
    headers?: {
        /**
         * The Content-Language request HTTP header defines a language or multiple languages of the request body.
         *
         * * If the `Content-Language` header is set to `*`, the localized fields should be provided as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the `Content-Language` header is set to a specific language, the localized fields should be provided as strings.
         * * If the `Content-Language` header is empty, the endpoint interprets the localized fields as being provided in the default language or the languages defined in the Configuration Service.
         *
         * **Note**: You can provide the localized fields only in languages defined in the Configuration Service. In case the fields are provided in languages that are not defined in the Configuration Service, the request is rejected.
         *
         */
        'Content-Language'?: string;
    };
    path: {
        /**
         * Your Emporix tenant's name.**Note**: The tenant should always be written in lowercase.
         */
        tenant: string;
    };
    query?: {
        /**
         * The parameter is valid only for `PARENT_VARIANT` type. If true then variants are not automatically generated for the parent variant configuration.
         */
        skipVariantGeneration?: boolean;
        /**
         * Allows to control whether the change of the state should cause reindexing or not.
         */
        doIndex?: boolean;
        /**
         * If set to `true`, the validation that checks whether related products exist is skipped. This is useful during data loading, where related products may not yet exist and will be created later. This parameter is only respected when the `allowToSkipRelatedProductsValidation` system setting is set to `true`. Otherwise, it is disregarded.
         */
        skipRelatedItemsValidation?: boolean;
    };
    url: '/product/{tenant}/products/bulk';
};

export type PostProductCreateBulkProductsErrors = {
    /**
     * Resources cannot be created due to an error.
     */
    400: CoreErrorMessage;
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * There are three possible reasons:
     * 1. Product with given code already exists, please choose unique code for your product
     * 2. Optimistic locking failed. If user sends metadata/version attribute which is outdated (someone else updated product in the time user was performing his changes). User should retrieve the latest product data and retry the request.
     * 3. Optimistic locking failed. User did not provide metadata/version attribute in update request, but someone else updated product while it was internally handled by product service. Resending the same request can result in successful update, but the update can override recently persisted changes.
     *
     */
    404: CoreErrorMessageWithDetails;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type PostProductCreateBulkProductsError = PostProductCreateBulkProductsErrors[keyof PostProductCreateBulkProductsErrors];

export type PostProductCreateBulkProductsResponses = {
    /**
     * Example response
     */
    207: Array<BulkResponse>;
};

export type PostProductCreateBulkProductsResponse = PostProductCreateBulkProductsResponses[keyof PostProductCreateBulkProductsResponses];

export type PutProductUpdateBulkProductsData = {
    body?: ProductBulkUpdateBody;
    headers?: {
        /**
         * The Content-Language request HTTP header defines a language or multiple languages of the request body.
         *
         * * If the `Content-Language` header is set to `*`, the localized fields should be provided as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the `Content-Language` header is set to a specific language, the localized fields should be provided as strings.
         * * If the `Content-Language` header is empty, the endpoint interprets the localized fields as being provided in the default language or the languages defined in the Configuration Service.
         *
         * **Note**: You can provide the localized fields only in languages defined in the Configuration Service. In case the fields are provided in languages that are not defined in the Configuration Service, the request is rejected.
         *
         */
        'Content-Language'?: string;
    };
    path: {
        /**
         * Your Emporix tenant's name.**Note**: The tenant should always be written in lowercase.
         */
        tenant: string;
    };
    query?: {
        /**
         * The parameter is valid only for `PARENT_VARIANT` type. If true then variants are not automatically generated for the parent variant configuration.
         */
        skipVariantGeneration?: boolean;
        /**
         * Allows to control whether the change of the state should cause reindexing or not.
         */
        doIndex?: boolean;
        /**
         * If set to `true`, the validation that checks whether related products exist is skipped. This is useful during data loading, where related products may not yet exist and will be created later. This parameter is only respected when the `allowToSkipRelatedProductsValidation` system setting is set to `true`. Otherwise, it is disregarded.
         */
        skipRelatedItemsValidation?: boolean;
    };
    url: '/product/{tenant}/products/bulk';
};

export type PutProductUpdateBulkProductsErrors = {
    /**
     * Bad request due to validation, incorrect parameters, etc.
     */
    400: CoreErrorMessage;
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type PutProductUpdateBulkProductsError = PutProductUpdateBulkProductsErrors[keyof PutProductUpdateBulkProductsErrors];

export type PutProductUpdateBulkProductsResponses = {
    /**
     * Example response
     */
    207: Array<BulkResponse>;
};

export type PutProductUpdateBulkProductsResponse = PutProductUpdateBulkProductsResponses[keyof PutProductUpdateBulkProductsResponses];

export type PostProductTriggerDynamicVariantRecalculationData = {
    body: DynamicVariantRecalculationRequest;
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: never;
    url: '/product/{tenant}/products/recalculate';
};

export type PostProductTriggerDynamicVariantRecalculationErrors = {
    /**
     * Resources cannot be created due to an error.
     */
    400: CoreErrorMessage;
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type PostProductTriggerDynamicVariantRecalculationError = PostProductTriggerDynamicVariantRecalculationErrors[keyof PostProductTriggerDynamicVariantRecalculationErrors];

export type PostProductTriggerDynamicVariantRecalculationResponses = {
    /**
     * Recalculation jobs accepted. One job is created per unique root product resolved from the submitted IDs.
     *
     */
    202: DynamicVariantRecalculationResponse;
};

export type PostProductTriggerDynamicVariantRecalculationResponse = PostProductTriggerDynamicVariantRecalculationResponses[keyof PostProductTriggerDynamicVariantRecalculationResponses];

export type GetProductListRecalculationJobsData = {
    body?: never;
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * Filter jobs by status. If omitted, all jobs are returned.
         */
        status?: DynamicVariantRecalculationJobStatus;
    };
    url: '/product/{tenant}/products/recalculate/jobs';
};

export type GetProductListRecalculationJobsErrors = {
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type GetProductListRecalculationJobsError = GetProductListRecalculationJobsErrors[keyof GetProductListRecalculationJobsErrors];

export type GetProductListRecalculationJobsResponses = {
    /**
     * Jobs retrieved successfully.
     */
    200: Array<DynamicVariantRecalculationJobResponse>;
};

export type GetProductListRecalculationJobsResponse = GetProductListRecalculationJobsResponses[keyof GetProductListRecalculationJobsResponses];

export type GetProductGetRecalculationJobData = {
    body?: never;
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
        /**
         * Unique identifier of the recalculation job.
         */
        jobId: string;
    };
    query?: never;
    url: '/product/{tenant}/products/recalculate/jobs/{jobId}';
};

export type GetProductGetRecalculationJobErrors = {
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Resource has not been found.
     */
    404: ProductErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type GetProductGetRecalculationJobError = GetProductGetRecalculationJobErrors[keyof GetProductGetRecalculationJobErrors];

export type GetProductGetRecalculationJobResponses = {
    /**
     * Job retrieved successfully.
     */
    200: DynamicVariantRecalculationJobResponse;
};

export type GetProductGetRecalculationJobResponse = GetProductGetRecalculationJobResponses[keyof GetProductGetRecalculationJobResponses];

export type DeleteProductRemoveProductData = {
    body?: {
        [key: string]: unknown;
    };
    path: {
        /**
         * Product unique identifier generated when the product is created.
         */
        productId: string;
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * Removing a parent variant product also removes all of its child variants. This applies to both `PARENT_VARIANT` roots with `VARIANT` children and `DYNAMIC_VARIANT` parent products with all descendant dynamic variants in the tree. This operation has to be confirmed by setting `force=true`.
         *
         */
        force?: boolean;
        /**
         * Allows to control whether the change of the state should cause reindexing or not.
         */
        doIndex?: boolean;
    };
    url: '/product/{tenant}/products/{productId}';
};

export type DeleteProductRemoveProductErrors = {
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type DeleteProductRemoveProductError = DeleteProductRemoveProductErrors[keyof DeleteProductRemoveProductErrors];

export type DeleteProductRemoveProductResponses = {
    /**
     * The resource has been successfully deleted.
     *
     */
    204: void;
};

export type DeleteProductRemoveProductResponse = DeleteProductRemoveProductResponses[keyof DeleteProductRemoveProductResponses];

export type GetProductRetrieveProductData = {
    body?: never;
    headers?: {
        /**
         * The Accept-Language request HTTP header defines which languages the client is able to understand, and which locale variant is preferred. If empty, the default system language is assumed. It can be a priority list working as a fallback mechanism.
         */
        'Accept-Language'?: string;
    };
    path: {
        /**
         * Product unique identifier generated when the product is created.
         */
        productId: string;
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * Fields to be returned in the response.
         *
         * When this parameter is passed, only the `id`, `yrn` and `{fieldName}` are retrieved for each product.
         *
         * You can specify multiple fields by separating them with commas.
         *
         */
        fields?: string;
        /**
         * Fields that should be expanded with additional information in the response body. Expressed as strings separated by commas. You can combine multiple values, for example `expand=template,parentVariant,brand,labels`.
         *
         * Possible values:
         * * `template`
         * * `parentVariant`
         * * `brand`
         * * `labels`
         *
         */
        expand?: string;
        /**
         * Flag indicating whether product variant(s) should include their parent variants' attribute values.
         * * If set to `true`, the result contains values from both the variant and its parent variant.
         * * If set to `false`, the result contains only variant values.
         *
         */
        rawValue?: boolean;
    };
    url: '/product/{tenant}/products/{productId}';
};

export type GetProductRetrieveProductErrors = {
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Resource has not been found.
     */
    404: ProductErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type GetProductRetrieveProductError = GetProductRetrieveProductErrors[keyof GetProductRetrieveProductErrors];

export type GetProductRetrieveProductResponses = {
    /**
     * Product successfully retrieved.
     *
     */
    200: BasicProductWithId | BundleProductWithId | ParentVariantProductWithId | VariantProductWithId | DynamicVariantProductWithId;
};

export type GetProductRetrieveProductResponse = GetProductRetrieveProductResponses[keyof GetProductRetrieveProductResponses];

export type PatchProductUpdateProductData = {
    body?: ProductPartialUpdateBody;
    headers?: {
        /**
         * The Content-Language request HTTP header defines a language or multiple languages of the request body.
         *
         * * If the `Content-Language` header is set to `*`, the localized fields should be provided as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the `Content-Language` header is set to a specific language, the localized fields should be provided as strings.
         * * If the `Content-Language` header is empty, the endpoint interprets the localized fields as being provided in the default language or the languages defined in the Configuration Service.
         *
         * **Note**: You can provide the localized fields only in languages defined in the Configuration Service. In case the fields are provided in languages that are not defined in the Configuration Service, the request is rejected.
         *
         */
        'Content-Language'?: string;
    };
    path: {
        /**
         * Product unique identifier generated when the product is created.
         */
        productId: string;
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * The parameter is valid only for `PARENT_VARIANT` type. If true then variants are not automatically generated for the parent variant configuration.
         */
        skipVariantGeneration?: boolean;
        /**
         * Allows to control whether the change of the state should cause reindexing or not.
         */
        doIndex?: boolean;
        /**
         * If set to `true`, the validation that checks whether related products exist is skipped. This is useful during data loading, where related products may not yet exist and will be created later. This parameter is only respected when the `allowToSkipRelatedProductsValidation` system setting is set to `true`. Otherwise, it is disregarded.
         */
        skipRelatedItemsValidation?: boolean;
    };
    url: '/product/{tenant}/products/{productId}';
};

export type PatchProductUpdateProductErrors = {
    /**
     * Bad request due to validation, incorrect parameters, etc.
     */
    400: CoreErrorMessage;
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * The requested resource does not exist.
     *
     */
    404: CoreErrorMessage;
    /**
     * There are three possible reasons:
     * 1. Product with given code already exists, please choose unique code for your product
     * 2. Optimistic locking failed. If user sends metadata/version attribute which is outdated (someone else updated product in the time user was performing his changes). User should retrieve the latest product data and retry the request.
     * 3. Optimistic locking failed. User did not provide metadata/version attribute in update request, but someone else updated product while it was internally handled by product service. Resending the same request can result in successful update, but the update can override recently persisted changes.
     *
     */
    409: CoreErrorMessageWithDetails;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type PatchProductUpdateProductError = PatchProductUpdateProductErrors[keyof PatchProductUpdateProductErrors];

export type PatchProductUpdateProductResponses = {
    /**
     * The resource has been successfully updated.
     *
     */
    204: void;
};

export type PatchProductUpdateProductResponse = PatchProductUpdateProductResponses[keyof PatchProductUpdateProductResponses];

export type PutProductUpdateProductData = {
    body?: ProductUpdateBody;
    headers?: {
        /**
         * The Content-Language request HTTP header defines a language or multiple languages of the request body.
         *
         * * If the `Content-Language` header is set to `*`, the localized fields should be provided as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the `Content-Language` header is set to a specific language, the localized fields should be provided as strings.
         * * If the `Content-Language` header is empty, the endpoint interprets the localized fields as being provided in the default language or the languages defined in the Configuration Service.
         *
         * **Note**: You can provide the localized fields only in languages defined in the Configuration Service. In case the fields are provided in languages that are not defined in the Configuration Service, the request is rejected.
         *
         */
        'Content-Language'?: string;
    };
    path: {
        /**
         * Product unique identifier generated when the product is created.
         */
        productId: string;
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * | Option  | Description |
         * |---|---|
         * | `true` | A partial product update is performed. |
         * | `false` | A full product replacement is performed.|
         *
         */
        partial?: boolean;
        /**
         * The parameter is valid only for `PARENT_VARIANT` type. If true then variants are not automatically generated for the parent variant configuration.
         */
        skipVariantGeneration?: boolean;
        /**
         * Allows to control whether the change of the state should cause reindexing or not.
         */
        doIndex?: boolean;
        /**
         * If set to `true`, the validation that checks whether related products exist is skipped. This is useful during data loading, where related products may not yet exist and will be created later. This parameter is only respected when the `allowToSkipRelatedProductsValidation` system setting is set to `true`. Otherwise, it is disregarded.
         */
        skipRelatedItemsValidation?: boolean;
    };
    url: '/product/{tenant}/products/{productId}';
};

export type PutProductUpdateProductErrors = {
    /**
     * Bad request due to validation, incorrect parameters, etc.
     */
    400: CoreErrorMessage;
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Resource has not been found.
     */
    404: ProductErrorMessage;
    /**
     * There are three possible reasons:
     * 1. Product with given code already exists, please choose unique code for your product
     * 2. Optimistic locking failed. If user sends metadata/version attribute which is outdated (someone else updated product in the time user was performing his changes). User should retrieve the latest product data and retry the request.
     * 3. Optimistic locking failed. User did not provide metadata/version attribute in update request, but someone else updated product while it was internally handled by product service. Resending the same request can result in successful update, but the update can override recently persisted changes.
     *
     */
    409: CoreErrorMessageWithDetails;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type PutProductUpdateProductError = PutProductUpdateProductErrors[keyof PutProductUpdateProductErrors];

export type PutProductUpdateProductResponses = {
    /**
     * The resource has been successfully created.
     *
     */
    201: ResourceLocation;
    /**
     * The resource has been successfully updated.
     *
     */
    204: void;
};

export type PutProductUpdateProductResponse = PutProductUpdateProductResponses[keyof PutProductUpdateProductResponses];

export type GetProductListTemplatesData = {
    body?: never;
    headers?: {
        /**
         * Flag indicating whether the total number of retrieved results should be returned.
         */
        'X-Total-Count'?: boolean;
        /**
         * The Accept-Language request HTTP header defines which languages the client is able to understand, and which locale variant is preferred. If empty, the default system language is assumed. It can be a priority list working as a fallback mechanism.
         */
        'Accept-Language'?: string;
    };
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: {
        /**
         * The page number to be retrieved where the size of the pages must be specified by the pageSize parameter. The number of the first page is 1.
         */
        pageNumber?: number;
        /**
         * The number of documents being retrieved on the page.
         */
        pageSize?: number;
        /**
         * Fields to sort the response data by following order of the parameters from left to right.
         * Can contain multiple fields in format: `field name:sort direction` separated by a comma.
         * The colon with `sort direction` parameter is redundant and descending order is taken only if it is equal to `desc` or `DESC`.
         * Ascending order is considered in any other case.
         * While sorting by a localised field the following rule is taken into account:
         * 1. If a localized field contains information about a language, then the language should be used. For example `name.en:ASC`. In that case en language is used for sorting the name property. The language suffix takes precedence over `Accept-Language` header.
         * 2. If a localized field does not contain information about a language, then the `Accept-Language` header is used. For example `name:ASC` and `Accept-Language: de`. In that case `de` is used for sorting the name property.
         * 3. If a localized field does not contain information about language and `Accept-Language` header contains `*`, then the default language from configuration service is used as a language. For example `name:ASC` and `Accept-Language: *` and default language is pl then pl is used as a language for name property.
         * 4. If a localized field does not contains information about language and `Accept-Language` header does not exists in the request, then the default language from configuration service is used.  "
         *
         */
        sort?: string;
        /**
         * Standard query parameter used to search for specific values.
         * * Searching for an item by string property: `q=id:31065d5b-b62e`, where `id` is the field name and `31065d5b-b62e` is its required value.
         * * Searching for an item by localized field property: `q=name.en:T-s` where `name` is the name of the field, `en` is a language code and `T-s` is a required value of this field.   This query works only for localized fields, which are stored in a Map format where `key` is a language code and `value` is translation to particular language.   + Searching for items by date property. All numer-based property queries are valid also for dates. In that case the date should be placed within double quotes: `q=metadata.createdAt:(>="2021-05-18T07:27:27.455Z" AND <"2021-05-20T07:27:27.455Z")`   + Searching for items with non existing or empty property: `q=name.en:null` where `name.en` is a name of fields that has value `null`.   + Searching for items with existing property: `q=attributes:exists` where `attributes` is a name of field that has `non null` value.   + Searching for items by multiple specific values: `q=id:(5c3325baa9812100098ff48f,5c3325d1a9812100098ff494)` where `id` is name of field and strings within a bracket are it''s required value.   + Searching for items by multiple fields: `q=id:5c3325baa9812100098ff48f name.en:T-s` where `id` and ''name.en'' are the names of fields. All documents that contain given values of these fields are returned. Multiple fields separated by space can be specified. Multiple values for each field can be also specified in a format presented above.   + Searching for items with string fields conforming to a regex: `q=name.en:~ABCD12` or `q=name.en:(~AB CD)` - in case of searching for strings with space, where `name` is the name of field and `ABCD12` or `AB CD` is it''s querying regex.'
         *
         */
        q?: string;
    };
    url: '/product/{tenant}/product-templates';
};

export type GetProductListTemplatesErrors = {
    /**
     * Bad request due to validation, incorrect parameters, etc.
     */
    400: CoreErrorMessage;
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type GetProductListTemplatesError = GetProductListTemplatesErrors[keyof GetProductListTemplatesErrors];

export type GetProductListTemplatesResponses = {
    200: ProductTemplatesResponse;
};

export type GetProductListTemplatesResponse = GetProductListTemplatesResponses[keyof GetProductListTemplatesResponses];

export type PostProductCreateTemplateData = {
    body?: ProductTemplateCreation;
    headers?: {
        /**
         * The Content-Language request HTTP header defines a language or multiple languages of the request body.
         *
         * * If the `Content-Language` header is set to `*`, the localized fields should be provided as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the `Content-Language` header is set to a specific language, the localized fields should be provided as strings.
         * * If the `Content-Language` header is empty, the endpoint interprets the localized fields as being provided in the default language or the languages defined in the Configuration Service.
         *
         * **Note**: You can provide the localized fields only in languages defined in the Configuration Service. In case the fields are provided in languages that are not defined in the Configuration Service, the request is rejected.
         *
         */
        'Content-Language'?: string;
    };
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
    };
    query?: never;
    url: '/product/{tenant}/product-templates';
};

export type PostProductCreateTemplateErrors = {
    /**
     * Bad request due to validation, incorrect parameters, etc.
     */
    400: CoreErrorMessage;
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type PostProductCreateTemplateError = PostProductCreateTemplateErrors[keyof PostProductCreateTemplateErrors];

export type PostProductCreateTemplateResponses = {
    201: {
        /**
         * Identifier of the newly created product template.
         */
        id?: string;
    };
};

export type PostProductCreateTemplateResponse = PostProductCreateTemplateResponses[keyof PostProductCreateTemplateResponses];

export type DeleteProductRemoveTemplateData = {
    body?: {
        [key: string]: unknown;
    };
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
        /**
         * Unique identifier of the product template.
         */
        'product-template-id': string;
    };
    query?: never;
    url: '/product/{tenant}/product-templates/{product-template-id}';
};

export type DeleteProductRemoveTemplateErrors = {
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type DeleteProductRemoveTemplateError = DeleteProductRemoveTemplateErrors[keyof DeleteProductRemoveTemplateErrors];

export type DeleteProductRemoveTemplateResponses = {
    /**
     * No Content
     */
    204: void;
};

export type DeleteProductRemoveTemplateResponse = DeleteProductRemoveTemplateResponses[keyof DeleteProductRemoveTemplateResponses];

export type GetProductRetrieveTemplateData = {
    body?: never;
    headers?: {
        /**
         * The Accept-Language request HTTP header defines which languages the client is able to understand, and which locale variant is preferred. If empty, the default system language is assumed. It can be a priority list working as a fallback mechanism.
         */
        'Accept-Language'?: string;
    };
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
        /**
         * Unique identifier of the product template.
         */
        'product-template-id': string;
    };
    query?: {
        /**
         * Version of the product template. If the parameter is not provided, the latest version of product template is returned.
         */
        version?: string;
    };
    url: '/product/{tenant}/product-templates/{product-template-id}';
};

export type GetProductRetrieveTemplateErrors = {
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Resource has not been found.
     */
    404: ProductErrorMessage;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type GetProductRetrieveTemplateError = GetProductRetrieveTemplateErrors[keyof GetProductRetrieveTemplateErrors];

export type GetProductRetrieveTemplateResponses = {
    200: ProductTemplateResponse;
};

export type GetProductRetrieveTemplateResponse = GetProductRetrieveTemplateResponses[keyof GetProductRetrieveTemplateResponses];

export type PutProductUpdateTemplateData = {
    body?: ProductTemplateUpdate;
    headers?: {
        /**
         * The Content-Language request HTTP header defines a language or multiple languages of the request body.
         *
         * * If the `Content-Language` header is set to `*`, the localized fields should be provided as maps of translations, where the keys are language codes and values are the fields in their respective languages.
         * * If the `Content-Language` header is set to a specific language, the localized fields should be provided as strings.
         * * If the `Content-Language` header is empty, the endpoint interprets the localized fields as being provided in the default language or the languages defined in the Configuration Service.
         *
         * **Note**: You can provide the localized fields only in languages defined in the Configuration Service. In case the fields are provided in languages that are not defined in the Configuration Service, the request is rejected.
         *
         */
        'Content-Language'?: string;
    };
    path: {
        /**
         * Your Emporix tenant name.
         *
         * **Note**: The tenant should always be written in lowercase.
         *
         */
        tenant: string;
        /**
         * Unique identifier of the product template.
         */
        'product-template-id': string;
    };
    query?: never;
    url: '/product/{tenant}/product-templates/{product-template-id}';
};

export type PutProductUpdateTemplateErrors = {
    /**
     * Bad request due to validation, incorrect parameters, etc.
     */
    400: CoreErrorMessage;
    /**
     * Unauthorized
     */
    401: {
        fault?: {
            faultstring?: string;
            detail?: {
                errorcode?: string;
            };
        };
    };
    /**
     * Access forbidden. The caller is not allowed to access this resource.
     */
    403: CoreErrorMessage;
    /**
     * Resource has not been found.
     */
    404: ProductErrorMessage;
    /**
     * There are three possible reasons:
     * 1. Product with given code already exists, please choose unique code for your product
     * 2. Optimistic locking failed. If user sends metadata/version attribute which is outdated (someone else updated product in the time user was performing his changes). User should retrieve the latest product data and retry the request.
     * 3. Optimistic locking failed. User did not provide metadata/version attribute in update request, but someone else updated product while it was internally handled by product service. Resending the same request can result in successful update, but the update can override recently persisted changes.
     *
     */
    409: CoreErrorMessageWithDetails;
    /**
     * Internal Server Error.
     */
    500: CoreErrorMessage;
};

export type PutProductUpdateTemplateError = PutProductUpdateTemplateErrors[keyof PutProductUpdateTemplateErrors];

export type PutProductUpdateTemplateResponses = {
    /**
     * No Content
     */
    204: void;
};

export type PutProductUpdateTemplateResponse = PutProductUpdateTemplateResponses[keyof PutProductUpdateTemplateResponses];

export type ClientOptions = {
    baseUrl: 'https://api.emporix.io' | (string & {});
};